use bevy::prelude::*;
use bevy::app::AppExit;

use crate::AppState;

use crate::pause_menu::components::*;
use crate::pause_menu::styles::*;

pub fn interact_with_resume_button (
    // Systems
) {
    // Logic if Let ok pressed, hovered, normal
}

pub fn interact_with_mainmenu_button (
    // Systems (look at mainmenu interactions)
) {
    // Logic, If let Ok : pressed, hovered, normal
}

pub fn interact_with_quit_button (
    // Systems
) {
    // Logic, same as above // use app exit instead
}